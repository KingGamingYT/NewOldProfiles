import { locale } from '@common/locale'

export const headers = {
    0: locale.Strings.PLAYING_A_GAME(), // playing
    1: locale.Strings.STREAMING({ name: '' }), // streaming
    2: locale.Strings.LISTENING_TO({ name: '' }), // listening
    3: locale.Strings.WATCHING({ name: '' }), // watching
    5: locale.Strings.COMPETING_IN({ name: '' }) // competing
};